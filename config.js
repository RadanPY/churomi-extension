// Churomi defaults (safe to ship in extension)
// Free mode does not require account or checkout configuration.

/* global self */

self.CHUROMI_DEFAULTS = {
  siteUrl: 'https://churomi.com',
  backendUrl: 'https://api.churomi.com',
  firebaseApiKey: ''
};
